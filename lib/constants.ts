/** Recommendation loop rules (README "Recommendation Loop Rules"). */
export const MAX_ROUNDS = 3;
export const PATHS_PER_ROUND = 3;
export const MAX_PATHS = 9;

export const CLOSING_MESSAGE = `We've explored the strongest career directions that align with your experience, interests, and goals.

Rather than generating increasingly weaker recommendations, I recommend selecting one path so we can build a focused action plan around it.

Which path feels most exciting to you right now?`;
